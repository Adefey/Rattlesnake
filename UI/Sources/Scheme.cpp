#include "Scheme.h"

Scheme::Scheme() {}

Scheme::~Scheme() {}

void Scheme::add_block(Block block, size_t position) {
  int test;
  test = 0;
}

void Scheme::delete_block(size_t identificator) {
  int test;
  test = 0;
}

std::vector<Parameter> Scheme::get_required_vars() {
  std::vector<Parameter> test;
  return test;
}

std::vector<Parameter> Scheme::run() {
  std::vector<Parameter> test;
  return test;
}

QTScheme Scheme::make_qt_frames() {
  QTScheme test;
  return test;
}
